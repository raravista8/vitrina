node_modules/
*.log
.DS_Store
# dist/ INTENTIONALLY NOT IGNORED — this is a vendored package, dist
# is committed so landing can install via `file:` without a build step.
# The `!dist/` un-ignore overrides the root .gitignore line `dist/`.
!dist/
