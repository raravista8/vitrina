# Standard ignores
node_modules
.next
dist
out
coverage

# Canon prototypes — kept byte-identical to design handoff
tests/visual/canon-source/

# Playwright artefacts
playwright-report/
test-results/
tests/visual/__diff/
tests/visual/baselines/

# Tier 2a — Jinja-rendered customer-site fixture (regenerated by
# `infra/scripts/render-customer-fixture.py` on every CI run + local
# dev). Contents are derived, not source.
tests/visual/__fixtures__/
